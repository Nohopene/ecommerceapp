import 'package:flutter/material.dart';

class ReBioWidget extends StatelessWidget {
  static const String id = 'rebio-screen';
  const ReBioWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
